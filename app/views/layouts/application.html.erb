<!DOCTYPE html>
<html lang="fr">
  <head>

    <title><%= @titre %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="<%= @article_description %>" />
    <meta property="og:title" content="<%= @headline %>" />
    <meta property="og:image" content="<%= @img_url %>" />
    <meta property="og:type" content="Article" />
    <meta property="og:description" content="<%= @article_description %>" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:site_name" content="ConfianScore" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="google-site-verification" content="9vLNAautB_8HT2Db0YGiqYSUP1SYc0-6MgaCYTyc3lc" />
    <%= favicon_link_tag 'logo-v.ico' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/chart.js@2.9.1/dist/Chart.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@2.9.1/dist/Chart.min.js"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light">
      <div class="container">
        <%= link_to "ConfianScore", root_path(), class: "navbar-brand"  %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <%= render 'searches/form', search: Search.new %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to "Inspecter des sources", sources_path(), class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Créer une fiche", new_person_path(), class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Charte et fonctionnement", charte_path(), class: "nav-link" %>
            </li>
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to "Se déconnecter", destroy_user_session_path(), method: :delete, class: "nav-link" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Se connecter", new_user_session_path(), class: "nav-link" %>
              </li>
            <% end %>
          </ul>

        </div>
      </div>
    </nav>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
