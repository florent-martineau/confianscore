<p id="notice"><%= notice %></p>

<h1>Point Verites</h1>

<table>
  <thead>
    <tr>
      <th>Person</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @point_verites.each do |point_verite| %>
      <tr>
        <td><%= point_verite.person_id %></td>
        <td><%= point_verite.value %></td>
        <td><%= link_to 'Show', point_verite %></td>
        <td><%= link_to 'Edit', edit_point_verite_path(point_verite) %></td>
        <td><%= link_to 'Destroy', point_verite, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Point Verite', new_point_verite_path %>
