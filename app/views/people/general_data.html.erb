<%= form_tag(update_general_data_path(@person.id)) do %>

  <div class="field">
    <%= rich_text_area_tag :last_general_data, @last_general_data %>
  </div>

  <div class="field" hidden>
    <%= number_field_tag :person_id, @person_id %>
  </div>

  <div class="actions">
    <%= submit_tag "Ajouter une source" %>
  </div>
<% end %>
